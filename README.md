# homebrew-minicat

MiniCat 的私有 Homebrew tap。

## 安装

```bash
brew tap iKrelve/minicat
brew install --cask minicat
```

## 更新

```bash
brew update && brew upgrade --cask minicat
```

## 卸载

```bash
brew uninstall --cask --zap minicat
brew untap iKrelve/minicat
```
